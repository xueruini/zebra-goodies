% Verify unlabeled notes inside inner-mode containers (tabular, fbox,
% parbox, footnote) ARE recorded — restored after the inner-mode hack
% revert.  Each container holds one unlabeled note with a unique body
% so they don't collide via Case 1.
\documentclass{article}
\usepackage{zebra}

\begin{document}

\fbox{\todo{boxed}}

\parbox{4cm}{\todo{parboxed}}

\begin{tabular}{ll}
  A & \todo{tabular-cell} \\
\end{tabular}

\footnote{\todo{footnoted}}

Body \todo{plain}.

\ExplSyntaxOn
\int_compare:nNnF { \int_use:c { g__zebra_note_count_todo_int } } = { 5 }
  {
    \PackageError { zebra-test }
      { Inner-mode~unlabeled~not~recorded }
      { Expected~5~todo~notes:~fbox,~parbox,~tabular,~footnote,~plain. }
  }
\ExplSyntaxOff

\end{document}
