% Regression: labeled note in \section with \tableofcontents.
% TOC replay (label stripped by \protected@write) must be suppressed.
% \zebraref cross-reference must resolve after two passes.
\documentclass{article}
\usepackage{zebra}
\input{zebra-test}

\begin{document}

\tableofcontents

\section{Intro \todo{\label{zebra:toc}check heading wording}}

Reference: \zebraref{zebra:toc} on p.~\pageref{zebra:toc}.

% One section todo; its TOC replay must be suppressed.
\ZebraAssertCount{todo}{1}
\ZebraAssertTotal{1}

\end{document}
